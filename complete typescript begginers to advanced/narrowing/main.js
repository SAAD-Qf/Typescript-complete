"use strict";
function askage(age) {
    // narowing
    if (typeof age === "number") {
        let tempage = age.toFixed(2);
    }
    if (typeof age === "string") {
        let tempage = age.toUpperCase();
    }
}
